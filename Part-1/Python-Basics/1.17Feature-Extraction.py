# Mapping from textual data to the real valued vectors is called feature extraction.
# converting textdata to the numeric data



'''   Tf-Idf vectorizer


1) Term-Frequent(Tf) : number of times 't
' appears in a document/number of terms in the document.

2) Inverse Document Frequency(IDF) : log(N/n) ,where 'N' is the number of documents and 'n' is the number of documents a term 't' has appered in.


The IDE values of a rare words is high,whereas the IDE of a frequent word is low

Tf-Idf value of a term=Tf*Idf

'''

# (nead to learn)

# convert the textual data to feature vectors